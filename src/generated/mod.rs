// The generated projection is the compiler's, not the author's: its enums
// carry whatever the contract declares, so the size lint has nothing to act on.
#[allow(clippy::large_enum_variant)]
pub mod signal;
